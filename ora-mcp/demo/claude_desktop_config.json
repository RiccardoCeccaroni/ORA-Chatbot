{
  "//": "Paste this block into your claude_desktop_config.json (macOS: ~/Library/Application Support/Claude/claude_desktop_config.json | Windows: %APPDATA%\\Claude\\claude_desktop_config.json). Restart Claude Desktop after saving. The block below works on macOS/Linux; on Windows, replace 'command' with 'cmd' and prepend '/c' to args (see demo/README.md). NOTE: if you can use claude.ai in a browser, the simpler path is claude.ai/customize/connectors -- see demo/README.md Opzione 1.",
  "mcpServers": {
    "ora": {
      "command": "npx",
      "args": ["-y", "mcp-remote", "https://ora-mcp-claudeai.fly.dev/mcp", "--transport", "http-only"]
    }
  }
}
